[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers --disable-warnings --cov=app --cov-report=term-missing
markers =
    unit: fast unit tests
    integration: api integration tests
    e2e: end-to-end business scenarios
